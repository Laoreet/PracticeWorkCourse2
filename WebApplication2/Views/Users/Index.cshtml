@model IEnumerable<WebApplication2.DataObjects.User>

@{
    ViewData["Title"] = "Index";

    Paging paging = new Paging();
    int pageNum = 0;

    if (ViewBag.Paging != null)
    {
        paging = ViewBag.Paging;
        pageNum = paging.CurrentPage;
    }
}

<h1>Пользователи</h1>

<p>
    <a asp-action="Create" class="btn btn-sm btn-success">Добавить <i class="fa fa-plus-square fa-lg"></i></a>
</p>
<a asp-action="ExportExcel" class="btn btn-sm btn-success">Экспорт <i class="fa fa-download fa-lg"></i></a>


<partial name="_paging" model="@paging" />

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Login)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Password)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Email)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Level)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Login)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Password)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Email)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Level)
            </td>
            <td>
                    <a asp-action="Edit" asp-route-id="@item.UserId" class="btn btn-sm btn-info"><i class="fa fa-pencil fa-lg"></i></a>  |
                    <a asp-action="Details" asp-route-id="@item.UserId" class="btn btn-sm btn-primary ml-1"><i class="fa fa-info-circle fa-lg"></i></a> |
                    <a asp-action="Delete" asp-route-id="@item.UserId" class="btn btn-sm btn-danger ml-1"><i class="fa fa-trash-o fa-lg"></i></a>
            </td>
        </tr>
}
    </tbody>
</table>
