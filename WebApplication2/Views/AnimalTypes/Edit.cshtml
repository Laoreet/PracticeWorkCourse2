@model WebApplication2.DataObjects.AnimalType

@{
    ViewData["Title"] = "Edit";
}

<h1>Изменить</h1>

<h4>Вид животного @Model.AnimalTypeId</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="AnimalTypeId" />
            <div class="form-group">
                <label asp-for="AnimalTypeName" class="control-label"></label>
                <input asp-for="AnimalTypeName" class="form-control" />
                <span asp-validation-for="AnimalTypeName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Сохранить" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Назад</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
